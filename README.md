# HBCE Platform

**HERMETICUM - BLINDATA · COMPUTABILE · EVOLUTIVA**  
**HERMETICUM B.C.E. S.r.l.**

HBCE Platform is the public R&D gateway, documentation repository and operational route spine for the HERMETICUM B.C.E. research infrastructure.

The current platform model is:

```text
HERMETICUM B.C.E. Platform
→ public gateway and route spine

IPR Onboarding App
→ operational registration and verification application surface

IPR
→ first operational product

IPR Card
→ verified operational access key

JOKER-C2
→ AI Operational Runtime accessible through verified IPR identity

IPR AI Audit Trail
→ first MVP / pilot use case

Registry / Verify
→ minimized public proof layer
```

HBCE Platform is not designed as a simple email-and-password login surface.

It is designed as an operational identity onboarding gateway where a subject is registered, privately verified where applicable, assigned an IPR record, issued an IPR Card, connected to an operational proof layer and then allowed to access governed AI workflows where policy, access state and scope allow it.

The active public spine is:

```text
Platform
→ IPR Onboarding App
→ Onboarding
→ IPR
→ IPR Card
→ JOKER-C2
→ IPR AI Audit Trail
→ Registry / Verify
→ Boundary / Pilot layers
```

The market distinction is structural:

```text
Classic AI
→ email
→ password
→ subscription
→ model access

HBCE / JOKER-C2
→ identity onboarding
→ official document evidence
→ national tax identifier / national identification number
→ private verification package
→ IPR Card
→ operational certificate
→ governed AI access
```

Core public formula:

```text
Classic AI starts with an email.
JOKER-C2 starts with a verified IPR.
```

Institutional formula:

```text
JOKER-C2 is not accessed through a simple email account.
It is an operational AI runtime accessible through verified IPR identity.
```

Commercial formula:

```text
Verify who you are.
Receive your IPR Card.
Access governed AI.
```

---

## 1. Product hierarchy

The repository uses a strict distinction between platform, application layer, product, access credential, MVP, ecosystem, runtime and framework.

```text
HERMETICUM B.C.E. Platform
→ public IPR Onboarding Gateway

IPR Onboarding App
→ operational registration and verification surface

IPR
→ first operational product

IPR Card
→ operational identity card / verified access key inside HBCE

IPR AI Audit Trail
→ first MVP / pilot use case

HBCE
→ operational ecosystem where IPR works

JOKER-C2
→ AI Operational Runtime / governed runtime demonstrator

MATRIX
→ wider architectural framework
```

This distinction is central.

The first product to validate is not the whole MATRIX framework. The first product to validate is **IPR**.

The first visible access object derived from IPR is **IPR Card**.

The first MVP derived from IPR is **IPR AI Audit Trail**.

The first governed runtime connected to IPR is **JOKER-C2**.

MATRIX remains the wider architectural framework.

---

## 2. What the HBCE Platform is

HBCE Platform is the public access threshold of the HERMETICUM B.C.E. ecosystem.

It is not only a website, not only a documentation repository, not only a static gateway and not only a login page.

Its target function is to explain, route and expose the operational identity layer required before access to governed AI workflows.

The current target model is:

```text
subject registration
→ private identity verification
→ document evidence package
→ IPR generation
→ IPR Card issuance
→ operational certificate
→ EVT activation event
→ OPC proof receipt
→ minimized public proof reference
→ governed access to JOKER-C2
```

The Platform behaves like an identity onboarding gateway in user experience. It may be compared structurally to fintech-style onboarding flows, but it is not a bank, not a financial intermediary, not a regulated KYC provider and not a public identity authority.

The correct definition is:

```text
HBCE Platform is an IPR Onboarding Gateway for governed AI access.
```

The dedicated operational application surface is:

```text
IPR Onboarding App
https://hbce-ipr-onboarding-app.vercel.app/
```

---

## 3. What IPR is

IPR — Identity Primary Record — is an operational identity, attribution and proof record.

It is not only a login, not only an account, not only a hash, not only a document signature and not only a registry entry.

An IPR connects:

- verified subject reference;
- operational role;
- organization or context;
- operation;
- document or process;
- verifiable event;
- hash / proof reference;
- continuity of trace;
- responsibility ownership;
- governed runtime access.

In practical terms, IPR is designed to answer:

- who operated;
- under which role;
- inside which system;
- on which document or process;
- with which responsibility;
- with which event;
- with which proof;
- with which verifiable continuity;
- under which access state;
- through which operational identity record.

A login says who entered a system.

A log says that something happened.

A signature says who signed.

A hash says whether data changed.

IPR connects identity, operation, event, proof, responsibility, continuity and governed access into one verifiable operational trace.

---

## 4. Classic AI vs JOKER-C2

The Platform is structured to make a clear distinction between classic AI access and governed AI access.

```text
Classic AI
→ email
→ password
→ payment
→ access to model

JOKER-C2
→ identity registration
→ document evidence
→ photo / video or liveness verification where applicable
→ national tax identifier / national identification number
→ IPR_PENDING state
→ verification review
→ IPR_ACTIVE state
→ IPR Card
→ operational certificate
→ governed AI access
```

Classic AI sells access to a model.

JOKER-C2 is designed to expose access to an identified, traced, auditable and responsibility-preserving AI operational runtime.

The access distinction is:

```text
Classic AI account
→ generic user profile

JOKER-C2 access
→ verified operational subject
```

The operational distinction is:

```text
Classic AI usage
→ prompt / output interaction

JOKER-C2 usage
→ IPR identity reference
→ context classification
→ policy evaluation
→ risk evaluation
→ governed response
→ EVT event
→ OPC proof
→ audit-ready continuity
```

---

## 5. IPR onboarding flow

The target onboarding flow is:

```text
registration
→ identity evidence
→ private verification package
→ provisional IPR
→ verification review
→ IPR Card issuance
→ operational certificate
→ governed runtime access
```

The intended user-facing sequence is:

1. Register.
2. Provide identity evidence.
3. Confirm national identifier where applicable.
4. Complete photo / video / liveness verification where applicable.
5. Receive `IPR_PENDING`.
6. Complete verification review.
7. Receive `IPR_ACTIVE`.
8. Download or store IPR Card / operational certificate.
9. Access JOKER-C2 where policy and access state allow it.

The onboarding model may use:

- email;
- phone number;
- password or passkey;
- country;
- language;
- subject type;
- official identity document category;
- national tax identifier / national identification number;
- private evidence package;
- verification status;
- activation event;
- proof reference.

Subject types may include:

- natural person;
- legal entity;
- operator;
- company;
- institution;
- professional;
- researcher.

Accepted identity-document categories for a human IPR may include:

- identity card / CIE;
- passport;
- driving license;
- residence permit where applicable.

For European and international use, the identifier language should remain neutral:

```text
national tax identifier / national identification number
```

This avoids assuming that all Member States use the same tax-code or national-identity structure.

---

## 6. IPR states

The Platform should treat IPR as a stateful operational identity record.

Core states:

```text
IPR_DRAFT
→ registration started but not submitted

IPR_PENDING
→ evidence submitted and waiting for review

IPR_REVIEW_REQUIRED
→ verification requires additional review or correction

IPR_ACTIVE
→ operational identity verified inside HBCE

IPR_SUSPENDED
→ temporary restriction due to review, risk or policy condition

IPR_REVOKED
→ no longer valid for operational access

IPR_EXPIRED
→ validity period ended and renewal required
```

Runtime access interpretation:

```text
NO_IPR
→ ACCESS_DENIED

IPR_DRAFT
→ ACCESS_DENIED

IPR_PENDING
→ ACCESS_DENIED / REVIEW_REQUIRED

IPR_REVIEW_REQUIRED
→ ACCESS_DENIED / HUMAN_REVIEW_REQUIRED

IPR_ACTIVE
→ ACCESS_ALLOWED

IPR_SUSPENDED
→ ACCESS_DENIED

IPR_REVOKED
→ ACCESS_DENIED

IPR_EXPIRED
→ ACCESS_DENIED / RENEWAL_REQUIRED
```

JOKER-C2 must not be presented as a generic AI surface accessible only by email and subscription.

Its correct access rule is:

```text
verified IPR required
```

---

## 7. IPR verification levels

The Platform may use verification levels for progressive operational trust.

```text
L0
→ unverified technical or local R&D reference

L1
→ basic account and contact verification

L2
→ document-based private verification

L3
→ enhanced verification with document, national identifier, photo / video / liveness and manual or controlled review
```

Verification levels are operational levels inside HBCE.

They do not create legal identity, public authority validation, eIDAS qualification, regulated certification, qualified electronic signature status or official identity status by themselves.

---

## 8. IPR Card

The IPR Card is the user-visible operational identity card inside HBCE.

It is not a state identity document.

It is not a passport.

It is not a CIE.

It is not SPID.

It is not an EUDI Wallet.

It is not a qualified eIDAS certificate.

It is not a regulated banking card.

It is an internally issued HBCE operational card that represents a verified IPR state and allows controlled access to HBCE workflows and JOKER-C2 where the access policy allows it.

The IPR Card may contain:

- public subject label;
- IPR-ID;
- subject type;
- country;
- verification level;
- document verified status;
- national tax identifier / national identification number status;
- issuer;
- activation EVT;
- OPC receipt reference;
- public hash reference;
- QR verification reference;
- issue date;
- expiration date;
- status.

Recommended card fields:

```text
IPR-ID
Subject label
Subject type
Country
Verification level
Document verified: yes / no
National identifier verified: yes / no
Issuer: HERMETICUM B.C.E. S.r.l.
Activation EVT
OPC receipt
Public hash reference
QR verification reference
Issued at
Expires at
Status: ACTIVE / PENDING / SUSPENDED / REVOKED / EXPIRED
```

Correct legal boundary:

```text
IPR Card is a digital operational card issued within the HERMETICUM B.C.E. environment.
It does not replace an identity card, passport, CIE, SPID, EUDI Wallet or qualified eIDAS certificate, unless future integrations with recognized identity or trust-service systems are separately established.
```

Correct institutional positioning:

```text
HBCE does not issue official European identity.
HBCE issues a verifiable operational identity reference that may be connected to official European identity systems in future compliant integrations.
```

---

## 9. Private verification and public proof minimization

The Platform separates private identity verification from public proof.

The private verification layer may use:

- email;
- phone;
- tax code where applicable;
- national tax identifier;
- national identification number;
- valid identity document;
- identity card / CIE;
- passport;
- driving license;
- residence permit where applicable;
- selfie;
- short video;
- liveness evidence;
- controlled evidence package;
- manual or semi-automated review notes.

The public proof layer must not contain:

- tax code;
- national identification number;
- identity document number;
- passport number;
- driving license number;
- residence permit number;
- identity document scan;
- portrait image;
- selfie;
- liveness video;
- address;
- date of birth;
- raw personal identifier;
- private evidence;
- customer file;
- private prompt;
- private AI output;
- production log;
- sensitive operational payload.

The public layer may contain minimized proof references such as:

- IPR reference;
- verification level;
- proof scope;
- `payload_sha256`;
- EVT reference;
- OPC reference;
- timestamp;
- status;
- public non-sensitive label;
- revocation or suspension status where applicable.

The correct public model is:

```text
private verification
→ private evidence package
→ hash-oriented proof reference
→ minimized registry entry
→ deterministic verification
```

The public registry does not store identity documents, tax codes, document numbers, scans, personal payloads or private evidence.

It stores only minimized proof references.

---

## 10. IPR profiles

The current product model is organized into profiles.

### IPR Base

A technical R&D profile for local testing, internal proof flows, hash-oriented attribution and controlled demonstrations.

### IPR Verified

A verified operational identity record created through private identity verification using document evidence and national identifier evidence where applicable.

### IPR Professional

A professional, business or audit workflow profile based on IPR Verified, intended for controlled B2B pilots, AI governance workflows and audit-ready evidence chains.

### IPR Operator

A controlled operational role profile for subjects authorized to perform review, issuance, audit, support or operational tasks inside HBCE workflows.

### IPR Entity

A future legal-entity or organization-linked profile for companies, institutions, research bodies or structured teams.

None of these profiles creates legal identity, regulated certification, eIDAS qualification, qualified trust-service status, banking status, KYC-provider status or public authority validation by itself.

---

## 11. Functional analogy

IPR can be understood through a functional analogy.

An IBAN identifies a bank account inside the financial system.

A tax code identifies a subject inside the administrative and fiscal system.

A digital signature validates an electronic act.

A digital credential allows access to a service.

A platform account allows a user to enter an application.

IPR is different from all of these, but it plays a similar ordering role inside a governed digital ecosystem.

It identifies an operational subject inside HBCE and connects that subject not only to access, but to operations, events, proofs, responsibility and continuity.

IPR does not claim the current legal status of a tax code, the banking function of an IBAN, the official status of a public identity document, the regulated effect of a qualified digital signature, the official status of an EUDI Wallet or the status of a qualified trust-service certificate.

The analogy is functional.

Complex systems require stable reference instruments to organize identity, operations and responsibility.

Inside HBCE, that operational reference instrument is IPR.

---

## 12. IPR creation model

The target operating model is that an IPR is created through HERMETICUM B.C.E. Platform and the IPR Onboarding App using private verification and hash-oriented public proof.

The intended flow is:

```text
private subject verification
→ valid identity evidence
→ national tax identifier / national identification number where applicable
→ private evidence package
→ IPR generation
→ IPR_PENDING state
→ verification review
→ IPR_ACTIVE state
→ IPR Card issuance
→ operational certificate
→ initial EVT event
→ OPC / hash / proof reference
→ minimized public registry reference
→ HBCE operating context
→ governed use in controlled workflows
```

The Platform is not meant to be only a login surface.

Its target function is to create an operational reference that can be used inside HBCE to connect identity, role, operation, event, proof, responsibility and governed AI access.

---

## 13. JOKER-C2 access model

JOKER-C2 is the AI Operational Runtime connected to the HBCE identity and proof model.

JOKER-C2 is not positioned as a generic chatbot accessed by email and password.

It demonstrates:

- AI operational identity;
- IPR-based access control;
- IPR attribution;
- context classification;
- governance decisioning;
- fail-closed behavior;
- EVT event generation;
- OPC proof generation;
- audit-oriented traceability;
- continuity of state;
- responsibility-preserving AI use.

The intended runtime access rule is:

```text
No verified IPR
→ no governed runtime access
```

Access sequence:

```text
HBCE Platform
→ IPR Onboarding App
→ IPR Card
→ operational certificate
→ JOKER-C2 Access Gate
→ AI Operational Runtime
```

Public runtime:

```text
https://hbce-ai-joker-c2.vercel.app/interface
```

The public runtime remains an R&D demonstrator unless and until a production deployment is separately scoped, reviewed, secured and contractually authorized.

---

## 14. IPR AI Audit Trail MVP

IPR AI Audit Trail is the first MVP extracted from the IPR / HBCE / JOKER-C2 / MATRIX architecture.

The route currently used for this MVP is:

```text
matrix-audit-trail/
```

The route name remains for deployment continuity, but the product interpretation is now:

```text
IPR AI Audit Trail
```

The MVP demonstrates a controlled AI-assisted document workflow where the following elements are connected:

```text
verified IPR reference
→ document upload
→ document SHA-256
→ AI-assisted analysis
→ governance decision
→ human validation
→ EVT event
→ OPC proof
→ audit-ready report
```

The first pilot client is HERMETICUM B.C.E. S.r.l. itself, using the system as an internal R&D self-audit pilot before any external customer pilot.

The MVP is not a generic AI document tool.

It is a demonstration of how an identified subject can use AI in a process that remains attributable, traceable, governable and reviewable.

---

## 15. Repository purpose

The purpose of this repository is to document and test a public-facing infrastructure model for systems whose actions, records, identities and governance states remain reconstructible, attributable and verifiable over time.

HBCE Platform does not present AI as an autonomous substitute for human or institutional responsibility.

It presents AI as a decision-support and audit-support layer that must remain connected to:

- IPR identity reference;
- IPR Card status;
- human validation;
- auditability;
- policy constraints;
- evidence continuity;
- responsibility ownership;
- fail-closed verification;
- revocation and suspension logic;
- public proof minimization.

---

## 16. Public platform spine

The current public spine should follow this logic:

```text
Platform
→ IPR Onboarding App
→ Onboarding
→ IPR
→ IPR Card
→ Create / Activate
→ JOKER-C2
→ IPR AI Audit Trail
→ Protocol
→ Registry v3
→ Verify
→ Verify Pack
→ Tools
→ Link Audit
→ Claims / Legal / Privacy / Terms / Security / Compliance / Governance
```

Primary public routes:

```text
/
onboarding/
ipr/
what-is-ipr/
ipr-card/
create/
activate/
joker-c2/
ai-joker-c2/
matrix-audit-trail/
protocol/
registry/
verify/
verify-pack/
tools/
tools/link-audit/
claims/
legal/
privacy/
terms/
security/
compliance/
governance/
pricing/
buy/
enterprise/
b2g/
industry/
partners/
contact/
```

External operational app:

```text
https://hbce-ipr-onboarding-app.vercel.app/
```

Some route names still contain MATRIX terminology for deployment continuity.

Their current interpretation is governed by this README:

```text
MATRIX = architectural framework
IPR = first operational product
IPR Card = operational access key
IPR AI Audit Trail = first MVP
JOKER-C2 = AI Operational Runtime
HBCE Platform = IPR Onboarding Gateway
IPR Onboarding App = operational app surface
```

---

## 17. Governance posture

The Platform follows these principles:

```text
EU_FIRST
AUDIT_FIRST
FAIL_CLOSED
HASH_ORIENTED
APPEND_ONLY
GDPR_MIN
PRIVATE_VERIFICATION
PUBLIC_PROOF_MINIMIZATION
VERIFIED_IPR_ACCESS
NO_PUBLIC_DATA_CUSTODY
NO_PRIVATE_KEY_CUSTODY
NO_FINANCIAL_CUSTODY
NO_BANKING_SERVICE
NO_REGULATED_KYC_PROVIDER_CLAIM
NO_REGULATED_CERTIFICATION_CLAIM
NO_PUBLIC_AUTHORITY_CLAIM
NO_EIDAS_QUALIFIED_TRUST_SERVICE_CLAIM
```

Operational interpretation:

```text
missing source → NON_OPERATIONAL
malformed source → NON_OPERATIONAL
ambiguous state → NON_OPERATIONAL
invalid hash → INVALID
no public match → NO_PUBLIC_RECORD
public hash match → PUBLIC_REFERENCE_PRESENT
no verified IPR → ACCESS_DENIED
IPR pending → REVIEW_REQUIRED
IPR active → ACCESS_ALLOWED
IPR suspended → ACCESS_DENIED
IPR revoked → ACCESS_DENIED
IPR expired → RENEWAL_REQUIRED
```

A public match means only that a public proof reference exists.

It does not certify legal validity, cybersecurity assurance, institutional approval, regulated compliance, operator qualification, production authorization, official identity status or qualified trust-service status.

---

## 18. Registry v3

The registry layer is aligned to the current R&D public proof boundary.

Current registry model:

- hash-oriented public proof references;
- manual append-only discipline;
- privacy-minimal metadata;
- fail-closed verification;
- no public identity document custody;
- no public tax-code custody;
- no public national identifier custody;
- no public photo or video custody;
- no private key custody;
- no financial custody;
- no banking service;
- no regulated certification claim;
- no public authority claim;
- no qualified trust-service claim.

The registry supports the IPR operating model by maintaining public proof references for selected R&D records.

Main registry files and routes:

```text
registry/
registry/registry.json
registry/ledger.json
registry/ipr_registry.json
registry/operators.json
registry/nodes.json
registry/events.json
registry/append/
registry/publish/
registry/operators/
registry/operators/append/
registry/operators/receipt/
registry/operators/pack/
registry/operators/status/
```

The registry is not a public authority registry, not a regulated identity registry and not a qualified trust service.

It is an R&D reference surface for public proof references, append-only discipline and fail-closed verification behavior.

---

## 19. Protocol references

The protocol surface is maintained as an R&D reference layer.

Current interpretation:

```text
RND_PROTOCOL_REFERENCE_BUNDLE
non-custodial R&D reference
IPR-first product hierarchy
IPR Card operational access model
IPR AI Audit Trail MVP boundary
JOKER-C2 governed runtime access model
not a legal standard
not a regulated certification scheme
not an eIDAS qualified trust service
not a public authority registry
not a production compliance framework by itself
```

Protocol files:

```text
protocol/
protocol/hbce-protocol-v1.official.json
protocol/hbce-registry-ledger.example.json
protocol/hbce-sign.reference.js
protocol/hbce-verify.reference.js
protocol/hbce-network-architecture-v1.md
protocol/hbce-node-spec-v1.md
protocol/hbce-registry-spec-v1.md
protocol/ipr-event-v1.md
```

---

## 20. Verification surfaces

The verification layer is aligned to:

- public proof reference lookup;
- local SHA-256 calculation;
- registry v3 source check;
- IPR Card public reference verification;
- EVT reference verification;
- OPC receipt reference verification;
- fail-closed validation;
- private evidence / public proof separation;
- no public data custody;
- no regulated certification claim;
- no public authority claim.

Verification routes:

```text
verify/
verify/verify.html
verify-pack/
verify/receipt/
verify/schema/evidence-bundle.schema.json
verify/demo-evidence.json
verify/baseline.md
```

Verification result interpretation:

```text
PUBLIC_REFERENCE_PRESENT
→ a public proof reference exists

NO_PUBLIC_RECORD
→ no matching public proof reference was found

NON_OPERATIONAL
→ the registry or verifier state is invalid

INVALID
→ the input is missing, malformed, ambiguous or rejected
```

Verification does not expose the private identity file.

Verification checks only minimized public proof references.

---

## 21. EVT and OPC

EVT is the verifiable event layer.

An EVT event can represent:

- registration;
- document submission;
- identity review;
- IPR creation;
- IPR activation;
- IPR suspension;
- IPR revocation;
- IPR renewal;
- AI operation;
- validation;
- audit;
- block;
- execution;
- proof;
- IPR derivation;
- state transition.

OPC is the operational proof layer connected to events.

An OPC proof can include:

- event hash;
- document hash;
- AI output hash;
- IPR reference;
- IPR Card reference;
- EVT reference;
- timestamp;
- verification state;
- signature reference;
- anchor reference;
- audit report reference;
- access decision reference.

The role of OPC is to move from simple event logging to operational proof.

---

## 22. Tools and guardrails

The tools layer contains R&D verification utilities.

Current tools:

```text
tools/
tools/link-audit/
tools/registry-guard.js
```

The Link Audit separates:

```text
ACTIVE SPINE MISSING ROUTES
IPR ONBOARDING APP REFERENCES
ACTIONABLE LEGACY TEXT REFERENCES
EXPECTED LEGACY / GUARDRAIL REFERENCES
BROKEN OR UNREACHABLE INTERNAL ROUTES
BAD ROUTE REFERENCES
```

This prevents false audit failure when legacy terms appear only in:

- archival audit reports;
- schema forbidden-field lists;
- verification guardrails;
- registry guard code;
- link-audit pattern lists.

Current Tool Link Audit result after the IPR Onboarding App refactor:

```text
ACTIVE SPINE MISSING ROUTES → OK
IPR ONBOARDING APP REFERENCES → OK
BAD ROUTE REFERENCES → OK
ACTIONABLE LEGACY TEXT REFERENCES → OK
CONTEXTUAL LEGACY TEXT REFERENCES → OK
BROKEN OR UNREACHABLE INTERNAL ROUTES → OK
AUDIT INTERPRETATION → PASS
```

Future tools may include:

- IPR Card preview;
- QR proof generator;
- IPR access-state simulator;
- onboarding evidence checklist;
- public proof package builder.

Any tool handling sensitive identity evidence must remain outside the public proof layer or fail closed.

---

## 23. Public data policy

The public repository must not contain:

- API keys;
- private keys;
- passwords;
- authentication tokens;
- production secrets;
- tax codes;
- national identification numbers;
- identity document numbers;
- passport numbers;
- driving license numbers;
- residence permit numbers;
- private identity documents;
- identity document scans;
- selfies;
- liveness videos;
- confidential customer data;
- private communications;
- production logs containing personal data;
- raw AI prompts containing sensitive information;
- raw AI outputs containing sensitive information;
- client files;
- sensitive operational payloads.

Public verification materials should use minimal metadata and hash-oriented references wherever possible.

If a verification workflow requires sensitive data, the public layer must fail closed and require private or manual review.

---

## 24. Security and audit baseline

This repository is subject to a defensive security and governance audit process.

The audit scope includes:

- repository-owned code and documentation;
- GitHub Pages static deployment assumptions;
- registry integrity and hash-oriented verification logic;
- fail-closed behavior for missing or invalid registry matches;
- IPR access-state consistency;
- revocation and suspension logic;
- documentation consistency;
- environment, secret, dependency and deployment risk review;
- public route-layer consistency.

The audit is strictly defensive, authorized, non-offensive and limited to repository-owned systems and materials.

It does not include exploitation, malware development, credential theft, evasion, unauthorized access, destructive testing, data exfiltration or offensive cybersecurity operations.

Security-sensitive onboarding capabilities such as document verification, liveness detection, biometric comparison, private evidence storage, production authentication and certificate issuance require dedicated legal, privacy, cybersecurity and operational review before real deployment.

---

## 25. Commercial and pilot boundary

The current commercial posture is pilot-first.

The recommended first commercial path is:

```text
IPR Base
→ IPR Verified
→ IPR Card
→ IPR AI Audit Trail pilot
→ sample audit-ready reports
→ EVT / OPC references
→ verification review
→ production-readiness assessment
→ optional follow-up scope
```

Indicative pricing and pilot pages are for R&D scoping and partner review only.

They do not create:

- automatic purchase;
- binding public offer;
- software license issuance;
- regulated certification;
- eIDAS qualification;
- regulated KYC / AML provider relationship;
- banking service;
- financial custody;
- public authority approval;
- production deployment;
- production-grade enterprise SaaS readiness.

Any real pilot, customer test, institutional use, enterprise use or private identity-verification workflow requires a separate written scope, legal review, privacy review, cybersecurity review, contractual definition and operational validation.

---

## 26. Boundary and non-claims

HBCE Platform is an R&D reference platform.

It does not claim:

- legal certification;
- regulatory approval;
- eIDAS qualification;
- public authority approval;
- public-sector adoption;
- external audit certification;
- automated compliance;
- legally binding evidence status by itself;
- financial or banking authorization;
- banking service status;
- payment institution status;
- electronic money institution status;
- production-grade enterprise SaaS readiness;
- qualified trust service status;
- regulated identity provider status;
- regulated KYC / AML provider status;
- cybersecurity certification;
- operator certification;
- public authority registry status;
- official European identity issuance;
- replacement of identity card, passport, CIE, SPID, EUDI Wallet or qualified eIDAS certificate.

Any production deployment, external customer pilot, public-sector use, regulated compliance positioning or institutional integration requires independent legal, cybersecurity, privacy, compliance and operational review.

---

## 27. Public project gateway

Public gateway:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/
```

IPR Onboarding App:

```text
https://hbce-ipr-onboarding-app.vercel.app/
```

Primary product area:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/ipr/
```

IPR explainer:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/what-is-ipr/
```

IPR onboarding route:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/onboarding/
```

IPR Card route:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/ipr-card/
```

Current MVP route:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/matrix-audit-trail/
```

Registry:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/registry/
```

Verify:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/verify/
```

Tools:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/tools/
```

Tool Link Audit:

```text
https://manuelcoletta1-source.github.io/hermeticum-bce-platform/tools/link-audit/
```

JOKER-C2 runtime demonstrator:

```text
https://hbce-ai-joker-c2.vercel.app/interface
```

GitHub profile:

```text
https://github.com/manuelcoletta1-source
```

---

## 28. License and use

This repository is published for research, documentation and governance experimentation.

Any use of the materials should preserve the defensive, audit-oriented and responsibility-preserving nature of the project.

No part of this repository should be used to support unauthorized access, offensive cybersecurity activity, credential theft, malware development, evasion, data exfiltration or harmful automation.

---

## 29. Maintainer

Manuel Coletta  
HBCE Research  
HERMETICUM B.C.E. S.r.l.  
Torino, Italy, European Union




